services:
  app:
    build: .
    container_name: bookstore-app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/srv/data
    env_file:
      - .env
    environment:
      - DB_PATH=/srv/data/inquiries.db